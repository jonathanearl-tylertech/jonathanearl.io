import React from 'react';
import { Layout } from '../components/layout';
import { Skills } from '../components/skills';

export default () => {
  return (
    <Layout>
      <Skills/>
    </Layout>
  );
}